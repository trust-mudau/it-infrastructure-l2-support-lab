name: Scenario 14 - Azure NSG Configuration Troubleshooting

on:
  push:
    paths:
      - '.github/workflows/scenario-14-azure-nsg-configuration.yml'
  workflow_dispatch:

jobs:
  investigate-azure-config:
    name: Validate Azure NSG configuration and remediate missing HTTPS rule
    runs-on: ubuntu-24.04

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Prepare Azure Bicep scenario
        shell: bash
        run: |
          set -Eeuo pipefail
          EVIDENCE="$GITHUB_WORKSPACE/evidence-runtime/scenario-14"
          LAB="$RUNNER_TEMP/scenario14"
          mkdir -p "$EVIDENCE" "$LAB"
          if ! command -v az >/dev/null 2>&1; then
            curl -sL https://aka.ms/InstallAzureCLIDeb | sudo bash >/dev/null
          fi
          az version | tee "$EVIDENCE/azure-cli-version.txt"
          az bicep version | tee "$EVIDENCE/bicep-version.txt"
          cat > "$LAB/main.bicep" <<'BICEP'
          param location string = 'southafricanorth'
          resource nsg 'Microsoft.Network/networkSecurityGroups@2024-05-01' = {
            name: 'nsg-support-web'
            location: location
            properties: {
              securityRules: [
                {
                  name: 'Allow-SSH-Admin'
                  properties: {
                    priority: 100
                    access: 'Allow'
                    direction: 'Inbound'
                    protocol: 'Tcp'
                    sourcePortRange: '*'
                    destinationPortRange: '22'
                    sourceAddressPrefix: '10.10.0.0/24'
                    destinationAddressPrefix: '*'
                  }
                }
                {
                  name: 'Deny-All-Inbound'
                  properties: {
                    priority: 4096
                    access: 'Deny'
                    direction: 'Inbound'
                    protocol: '*'
                    sourcePortRange: '*'
                    destinationPortRange: '*'
                    sourceAddressPrefix: '*'
                    destinationAddressPrefix: '*'
                  }
                }
              ]
            }
          }
          BICEP
          az bicep build --file "$LAB/main.bicep" --outfile "$LAB/main.json"
          cp "$LAB/main.bicep" "$EVIDENCE/nsg-before.bicep"
          cp "$LAB/main.json" "$EVIDENCE/nsg-before.json"

      - name: Reproduce configuration-level HTTPS failure
        shell: bash
        run: |
          set -Eeuo pipefail
          EVIDENCE="$GITHUB_WORKSPACE/evidence-runtime/scenario-14"
          LAB="$RUNNER_TEMP/scenario14"
          COUNT="$(jq '[.resources[0].properties.securityRules[] | select(.properties.direction=="Inbound" and .properties.access=="Allow" and (.properties.protocol=="Tcp" or .properties.protocol=="*") and (.properties.destinationPortRange=="443" or .properties.destinationPortRange=="*"))] | length' "$LAB/main.json")"
          echo "inbound_allow_443_count=$COUNT" | tee "$EVIDENCE/rule-check-before.txt"
          if [[ "$COUNT" -ne 0 ]]; then
            echo 'Expected missing HTTPS rule was not reproduced.' >&2
            exit 1
          fi
          echo 'CONFIGURATION FAILURE: no inbound Allow rule permits TCP 443 before the deny-all rule.' | tee -a "$EVIDENCE/rule-check-before.txt"

      - name: Gather cloud configuration evidence
        shell: bash
        run: |
          set -Eeuo pipefail
          EVIDENCE="$GITHUB_WORKSPACE/evidence-runtime/scenario-14"
          LAB="$RUNNER_TEMP/scenario14"
          {
            echo '=== AZURE CLI ==='
            az version
            echo
            echo '=== BICEP SOURCE ==='
            cat "$LAB/main.bicep"
            echo
            echo '=== COMPILED NSG RULES ==='
            jq -r '.resources[0].properties.securityRules[] | [.name, (.properties.priority|tostring), .properties.access, .properties.direction, .properties.protocol, .properties.destinationPortRange, .properties.sourceAddressPrefix] | @tsv' "$LAB/main.json"
            echo
            echo '=== FAILED RULE CHECK ==='
            cat "$EVIDENCE/rule-check-before.txt"
          } | tee "$EVIDENCE/investigation.txt"

      - name: Correct NSG configuration
        shell: bash
        run: |
          set -Eeuo pipefail
          EVIDENCE="$GITHUB_WORKSPACE/evidence-runtime/scenario-14"
          LAB="$RUNNER_TEMP/scenario14"
          cat > "$LAB/main-fixed.bicep" <<'BICEP'
          param location string = 'southafricanorth'
          resource nsg 'Microsoft.Network/networkSecurityGroups@2024-05-01' = {
            name: 'nsg-support-web'
            location: location
            properties: {
              securityRules: [
                {
                  name: 'Allow-SSH-Admin'
                  properties: {
                    priority: 100
                    access: 'Allow'
                    direction: 'Inbound'
                    protocol: 'Tcp'
                    sourcePortRange: '*'
                    destinationPortRange: '22'
                    sourceAddressPrefix: '10.10.0.0/24'
                    destinationAddressPrefix: '*'
                  }
                }
                {
                  name: 'Allow-HTTPS-Web'
                  properties: {
                    priority: 200
                    access: 'Allow'
                    direction: 'Inbound'
                    protocol: 'Tcp'
                    sourcePortRange: '*'
                    destinationPortRange: '443'
                    sourceAddressPrefix: 'Internet'
                    destinationAddressPrefix: '*'
                  }
                }
                {
                  name: 'Deny-All-Inbound'
                  properties: {
                    priority: 4096
                    access: 'Deny'
                    direction: 'Inbound'
                    protocol: '*'
                    sourcePortRange: '*'
                    destinationPortRange: '*'
                    sourceAddressPrefix: '*'
                    destinationAddressPrefix: '*'
                  }
                }
              ]
            }
          }
          BICEP
          az bicep build --file "$LAB/main-fixed.bicep" --outfile "$LAB/main-fixed.json"
          cp "$LAB/main-fixed.bicep" "$EVIDENCE/nsg-after.bicep"
          cp "$LAB/main-fixed.json" "$EVIDENCE/nsg-after.json"
          {
            echo 'Confirmed root cause: the NSG definition had no inbound Allow rule for TCP 443, so the explicit deny-all rule would block HTTPS.'
            echo 'Corrective action: added an inbound TCP 443 Allow rule at priority 200 before the deny-all rule.'
            echo 'Scope note: this is Azure configuration/IaC validation only; no live Azure subscription or production resource was modified.'
          } | tee "$EVIDENCE/corrective-action.txt"

      - name: Verify corrected Azure configuration
        shell: bash
        run: |
          set -Eeuo pipefail
          EVIDENCE="$GITHUB_WORKSPACE/evidence-runtime/scenario-14"
          LAB="$RUNNER_TEMP/scenario14"
          HTTPS_PRIORITY="$(jq -r '.resources[0].properties.securityRules[] | select(.name=="Allow-HTTPS-Web") | .properties.priority' "$LAB/main-fixed.json")"
          DENY_PRIORITY="$(jq -r '.resources[0].properties.securityRules[] | select(.name=="Deny-All-Inbound") | .properties.priority' "$LAB/main-fixed.json")"
          COUNT="$(jq '[.resources[0].properties.securityRules[] | select(.name=="Allow-HTTPS-Web" and .properties.access=="Allow" and .properties.direction=="Inbound" and .properties.protocol=="Tcp" and .properties.destinationPortRange=="443")] | length' "$LAB/main-fixed.json")"
          [[ "$COUNT" -eq 1 ]]
          [[ "$HTTPS_PRIORITY" -lt "$DENY_PRIORITY" ]]
          {
            echo 'Bicep build: PASS'
            echo 'HTTPS allow rule present: PASS'
            echo "HTTPS priority=$HTTPS_PRIORITY"
            echo "Deny-all priority=$DENY_PRIORITY"
            echo 'HTTPS rule evaluated before deny-all by priority: PASS'
            echo 'Scenario result: CONFIGURATION RESOLVED'
          } | tee "$EVIDENCE/final-verification.txt"

      - name: Write incident summary
        shell: bash
        run: |
          cat > "$GITHUB_WORKSPACE/evidence-runtime/scenario-14/incident-summary.txt" <<'EOF'
          Incident: INC-014 Azure-Style Web Workload Configuration Missing HTTPS NSG Rule
          Impact: configuration review showed an Azure NSG would deny inbound HTTPS because only SSH was allowed before an explicit deny-all rule.
          Environment: Azure CLI and Bicep were used locally on a GitHub-hosted runner. This is configuration-level cloud exposure and not a claim of live Azure administration.
          Confirmed root cause: no inbound TCP 443 Allow rule existed in the compiled NSG definition.
          Corrective action: added Allow-HTTPS-Web at priority 200 before Deny-All-Inbound at priority 4096 and rebuilt the Bicep template.
          Verification: compiled JSON contained the expected HTTPS rule and priority ordering.
          Escalation decision: a production change would require cloud/network change approval and validation against source restrictions, application architecture and security policy before deployment.
          EOF

      - name: Upload evidence
        uses: actions/upload-artifact@v4
        with:
          name: scenario-14-evidence
          path: evidence-runtime/scenario-14/
          if-no-files-found: error
          retention-days: 30
